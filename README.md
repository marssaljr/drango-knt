# freemazon
```
git clone https://github.com/marssaljr/freemazon.git
cd freemazon
pip install --user virtualenv
python -m venv .
source ./bin/active
pip install -r requirements.txt 
python manage.py runserver
```
